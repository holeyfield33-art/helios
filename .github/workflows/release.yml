name: Release

'on':
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  test:
    name: Test before release
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Go vet
        run: go vet ./...

      - name: Go tests
        run: go test ./... -count=1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Python tests
        run: |
          pip install -e .[test] -q
          pytest implementations/python/tests -W error -q

  build-go-binaries:
    name: Build Go Binaries
    needs: test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        goos: [linux, darwin, windows]
        goarch: [amd64, arm64]
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Build binary
        run: |
          VERSION=${GITHUB_REF#refs/tags/}
          OS=${{ matrix.goos }}
          ARCH=${{ matrix.goarch }}
          EXT=""
          [[ "$OS" == "windows" ]] && EXT=".exe"
          OUTPUT="helios-${VERSION}-${OS}-${ARCH}${EXT}"
          CGO_ENABLED=0 GOOS=$OS GOARCH=$ARCH \
            go build \
              -ldflags "-X main.version=${VERSION}" \
              -o "$OUTPUT" ./cmd/helios/

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: go-binary-${{ matrix.goos }}-${{ matrix.goarch }}
          path: helios-*
          retention-days: 1

  build-python-package:
    name: Build Python Package
    needs: test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install build tools
        run: pip install build

      - name: Build wheel and sdist
        run: python -m build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: python-packages
          path: dist/
          retention-days: 1

  create-release:
    name: Create GitHub Release
    needs:
      - build-go-binaries
      - build-python-package
    runs-on: ubuntu-latest
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: release-artifacts

      - name: Prepare release assets and generate checksums
        run: |
          mkdir -p release
          find release-artifacts -type f -name "helios-*" \
            -exec cp {} release/ \;
          find release-artifacts -type f \
            \( -name "*.whl" -o -name "*.tar.gz" \) \
            -exec cp {} release/ \;
          cd release
          sha256sum * > SHA256SUMS.txt
          echo "--- release contents ---"
          cat SHA256SUMS.txt
          ls -lh

      - name: Create Release and Upload Assets
        uses: softprops/action-gh-release@v2
        with:
          files: release/*
          generate_release_notes: true
          fail_on_unmatched_files: true
